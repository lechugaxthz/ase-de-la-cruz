name = "ase-de-la-cruz"
compatibility_date = "2024-12-01"
pages_build_output_dir = "./dist"

[env.production]
vars = { ENVIRONMENT = "production" }

[env.preview]
vars = { ENVIRONMENT = "preview" }

# Umami Analytics (separate deployment)
# Deploy umami separately and add the script URL to BaseLayout.astro